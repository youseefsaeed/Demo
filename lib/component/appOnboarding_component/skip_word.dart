import 'package:flutter/material.dart';

class Skip_word extends StatelessWidget {
  const Skip_word({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Skip',
      style: TextStyle(
        color: Colors.black,
        fontSize: 14,
        fontFamily: 'TestFoundersGroteskText',
        fontWeight: FontWeight.w700,
        height: 1.42857142857,
      ),
    );
  }
}
